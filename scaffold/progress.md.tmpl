# progress

Append-only decision log. The agent writes one short bullet per task here
under "Notes per task". The loop halts on its own when every phase in
`tasks.md` has zero `[ ]` tasks left — no halt marker is required.

## Notes per task
